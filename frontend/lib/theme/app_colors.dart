import 'package:flutter/material.dart'; 


class AppColors {
  AppColors._(); 

  static const Color fundo = Color(0xFFEAF3FB); // Cor de fundo azul bem clara usada nas telas.
  static const Color fundoCartao = Colors.white; // Cor de fundo dos cartões (branco).
  static const Color azulPrimario = Color(0xFF2F80ED); // Azul principal da marca (botões, links, destaques).
  static const Color azulClaro = Color(0xFF56CCF2); // Azul mais claro, usado em detalhes e no logo.
  static const Color textoEscuro = Color(0xFF1B2733); // Cor escura usada nos textos principais.
  static const Color textoCinza = Color(0xFF8C9AAB); // Cinza usado em textos secundários (dicas, legendas).
  static const Color bordaCampo = Color(0xFFE1E8F0); // Cor da borda dos campos de texto.
  static const Color preenchimentoCampo = Color(0xFFF7FAFC); // Cor de fundo (preenchimento) dos campos de texto.
}
